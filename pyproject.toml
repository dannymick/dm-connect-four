[project]
name = "dm-connect-four"
version = "0.1.0"
description = "Add your description here"
entrypoint = "main:app"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "httpx2>=2.13.1",
    "pytest>=9.1.1",
]

[dependency-groups]
dev = []
